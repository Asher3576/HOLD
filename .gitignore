node_modules/
dist/
# 확장 빌드 결과물은 커밋 — 클론만 받으면 빌드 없이 chrome://extensions 에서 바로 로드
!extension/dist/
coverage/
*.local
.DS_Store

# Supabase CLI local state
supabase/.temp/
supabase/.branches/

# 시크릿 키는 절대 커밋 금지 (.env는 퍼블리셔블 키만 담으므로 커밋함)
.env.secret
.env.*.secret
