node_modules/
dist/
# 확장 빌드 결과물은 커밋 — 클론만 받으면 빌드 없이 chrome://extensions 에서 바로 로드
!extension/dist/
coverage/
*.local
.DS_Store

# Supabase CLI local state
supabase/.temp/
supabase/.branches/

# 환경 변수는 커밋하지 않는다 — web/.env.example 참고
.env
.env.*
!.env.example
