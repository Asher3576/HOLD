node_modules/
dist/
# 확장 빌드 결과물은 커밋 — 클론만 받으면 빌드 없이 chrome://extensions 에서 바로 로드
!extension/dist/
coverage/
*.local
.DS_Store

# Supabase CLI local state
supabase/.temp/
supabase/.branches/

# web/.env 는 퍼블리셔블(공개) 키만 담으므로 커밋한다 — 버셀이 이걸로 빌드.
# 시크릿 키는 절대 커밋 금지: 아래 패턴 파일에만 두거나 서버 시크릿으로.
.env.secret
.env.*.secret
.env.local
